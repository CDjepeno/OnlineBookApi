{
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "commitizen": "^4.3.1",
    "cz-customizable": "^7.3.0",
    "husky": "^8.0.0"
  },
  "scripts": {
    "lint:nest": "cd apps/nest && npm run lint",
    "lint:next": "cd apps/client-next && npm run lint",
    "prepare": "husky install",
    "build:nest": "cd apps/nest && npm run build",
    "build:all": "npm run build:next && npm run build:nest",
    "lint:all": "npm run lint:nest && npm run lint:next",
    "test:all": "",
    "lint:commit": "commitlint --from=HEAD~1"
  }
}
